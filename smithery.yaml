startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |
    (config) => ({
      command: "npx",
      args: ["-y", "@aan/mcp"],
      env: {}
    })
